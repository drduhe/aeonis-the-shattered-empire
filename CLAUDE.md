# Aeonis — Claude Code entry point

All durable project instructions live in AGENTS.md (single source of truth for every AI tool). It is imported below — read it before doing anything:

@AGENTS.md

Rules of this file:

- Keep this file a thin shim. New durable guidance goes in `AGENTS.md`, never here — duplicated instructions drift, and this project treats drift as a bug.
- Session-specific notes (`#` memory shortcuts) may land here temporarily; migrate anything worth keeping into `AGENTS.md` or the owning canon doc.
